# Vendored PageAgent Bundle

`pageagent.bundle.js` is the V0 vendored In-Page Engine fixture. Update it by replacing this file with a reviewed PageAgent browser bundle and rerunning the in-page engine tests.

The external `tweb` Text Protocol must remain Browser Subagent Task Turns and slash commands. PageAgent globals are an implementation detail inside the Active Page.
